global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:

  - job_name: "dcgm"
    static_configs:
      - targets:
          - "dcgm-exporter:9400"

  - job_name: "node"
    static_configs:
      - targets:
          - "node-exporter:9100"
